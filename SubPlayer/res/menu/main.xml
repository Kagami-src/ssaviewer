<menu xmlns:android="http://schemas.android.com/apk/res/android" >

    <item android:id="@+id/media_play"
        android:title="@string/info_play"
          android:icon="?attr/actionBarPlay"
          android:showAsAction="always"/>
     <item android:id="@+id/fullscreen"
         android:title="@string/info_fullscreen"
          android:icon="?attr/actionBarFullScreen"
          android:showAsAction="always"/>
     <item android:id="@+id/location"
         android:title="@string/info_location"
          android:icon="?attr/actionBarLocation"
          android:showAsAction="always"/>

</menu>
